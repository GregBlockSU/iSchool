# at the Windows command line
cd .\advanced-databases\
docker-compose start minio jupyter
docker-compose ps

# in the Jupyter console

in Jupyter console
# Connect to the Minio client. Create an alias to your Minio server, named ms. 
# Create a bucket labd. Inside the bucket, create folders iplookup and logs. 
# Copy the three log files from /datasets/clickstream to the Minio logs folder. 
# Copy iplookup.json to the iplookup folder.

wget https://dl.min.io/client/mc/release/linux-amd64/mc && chmod +x mc && sudo mv -f mc /usr/local/bin
mc alias set minio http://minio:9000 minio SU2orange!
mc mb minio/labd
mc mb minio/labd/logs
mc mb minio/labd/iplookup

mc cp /hpme/jovyan/datasets/clickstream/iplookup.json minio/labd/iplookup
mc cp /home/jovyan/datasets/clickstream/u_ex160211.log minio/labd/logs
mc cp /home/jovyan/datasets/clickstream/u_ex160212.log minio/labd/logs
mc cp /home/jovyan/datasets/clickstream/u_ex160213.log minio/labd/logs

mc ls minio/labd/logs
mc ls minio/labd/iplookup
